import { AstPath, Doc } from "prettier";
import { Node, MarkoEmbedNode } from "../constants";
type NodeWithEmbed = Node & {
    _embed?: MarkoEmbedNode;
};
export default function callEmbed(print: (path: AstPath<NodeWithEmbed>) => Doc, path: AstPath<NodeWithEmbed>, mode: string, code: string): Doc;
export {};
