import { Liquid } from '../../liquid';
import { Template } from '../../template/template';
import { Emitter, Context, ITagImplOptions, TagToken, ITemplate, Token } from '../../types';
export declare class Tag extends Template<TagToken> implements ITemplate {
    name: string;
    private impl;
    private static impls;
    constructor(token: TagToken, tokens: Token[], liquid: Liquid);
    render(ctx: Context, emitter: Emitter): IterableIterator<any>;
    static register(name: string, tag: ITagImplOptions): void;
    static clear(): void;
}
